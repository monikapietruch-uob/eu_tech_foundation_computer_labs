/*
  js/ai-config.js — where the AI proxy lives.

  After you deploy worker/ (see worker/README.md), paste the Worker's
  address between the quotes below and push. This address is not secret:
  the API key stays on Cloudflare. Leave it empty and the AI task page
  says the service is not set up yet.
*/
var AI_PROXY_URL = "";
